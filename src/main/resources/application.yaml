spring:
  application:
    name: k-middleware

# DB연동
  datasource:
    url: jdbc:mysql://localhost:3306/mes_database?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: mesuser
    password: qhdks!@34
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect